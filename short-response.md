# Short Response Questions

## 1. The first thing to do when collaborating with a classmate is to invite them into your repo because they need access to enter

## 2. Developers use branches to continue doing their work without affecting the main branch they are working on.

## 3. Git is how a devleoper see the code like a snapshot, Github is a cloud based service that allows devs to store, track, manage, and control their work. They work together to help the developer create coding

## 3. This command sequence is wrong because git add is first then we write our message with git commit -m lastly we git push

```
git add .
git commit -m "Saving work"
git push
```